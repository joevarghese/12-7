const http = require("http");
const express = require('express');


const app = express();
const server = http.createServer(app
c)



const app = express();
const server = http.createServer(app);
const port = 3001;